# Optional: when set, the app uses Clerk (sign-in + session JWTs on /api/*).
# 1) dashboard.clerk.com → your app → copy Publishable key into this file.
# 2) For the API: see backend .env.example “Clerk” (CLERK_ISSUER + CLERK_JWKS_URL from
#    the same “Frontend API” host as in Clerk → Configure → Developers).
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxx
